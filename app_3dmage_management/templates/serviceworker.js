// serviceworker.js
self.addEventListener('fetch', (event) => {
  // Questo Service Worker è il minimo indispensabile per far riconoscere l'app come PWA.
  // Non ha logica di caching, ma permette l'installazione.
});
